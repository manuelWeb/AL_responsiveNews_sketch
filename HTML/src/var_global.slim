/ ### !important créer les variable depuis source.json
-require 'json'
-file = File.read('source.json')
-$objso = JSON.parse(file)

/ Ruby dynamic tag and attribut
ruby:
  class Brand
    # attr_reader :name
    # attr_writer :name
    attr_accessor :name
    def initialize
      @name = ""
    end
    # Renvoie simplement @name
    def name=(un_nom)
      @name = un_nom
    end
  end

  class Tag
    def initialize(tag)
      @tag = tag
    end
    # return table bd:0 cp+cs:0 a:c
    def c0
      @table = {tag: @tag, border: '0', cellpadding: '0', cellspacing: '0', align: 'center'}
    end
    # return table bd:0 cp+cs:0 a:c
    def c0fullw
      @table = {tag: @tag, border: '0', cellpadding: '0', cellspacing: '0', align: 'center', width: '100%', class: 'mobile_320'}
    end
    # return td align:right
    def ar
      @td = {tag: @tag, align: 'right'}
    end
    # return td align:center
    def ac
      @td = {tag: @tag, align: 'center'}
    end
  end

/ - $brand = Brand.new($objso["brand"]["name"])
- $brand = Brand.new
- $brand.name = "Un autre nom de branding du groupe Domoti par exemple"
- $tab = Tag.new("table")
- $td = Tag.new("td")
